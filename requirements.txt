PySide2==5.14.0
shiboken2==5.14.0
setuptools>=56.0.0

# 安全依赖
cryptography>=3.4.8  # 必需：用于密码加密
secrets  # Python 3.6+ 内置，用于安全随机数生成

# 数据处理和分析
pandas>=1.3.0,<2.0  # 1.3.0+ 兼容 Python 3.8
numpy>=1.20.0,<1.24  # 兼容 Python 3.8

# 可选图表绘制（用于数据分析模块的图表功能）
# pip install pyqtgraph
pyqtgraph>=0.12.0; python_version >= "3.8"

# 可选 Excel 支持
# pip install openpyxl
openpyxl>=3.0.0; python_version >= "3.8"

# 测试依赖
pytest>=6.0
pytest-cov>=2.12
pytest-qt>=4.0  # PySide2 UI 测试

# 代码质量
flake8>=4.0
black>=21.0
isort>=5.0

# 服务器依赖
aiohttp>=3.8.0  # WebSocket服务器必需

# 应用程序配置
PyYAML>=6.0  # 用于配置文件解析（JSON/YAML）

# 可选数据库驱动（根据需要安装）
# Intersystems IRIS: pip install intersystems-irispython
# MySQL: pip install pymysql
# PostgreSQL: pip install psycopg2
# SQL Server: pip install pyodbc
# Oracle: pip install cx_Oracle
