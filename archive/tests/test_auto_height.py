#!/usr/bin/env python3
# -*- coding: utf-8 -*-

"""
测试控件高度自适应功能
集成了功能说明文档查看器
"""

import sys
from PySide2.QtWidgets import (
    QApplication, QTextEdit, QVBoxLayout, QDialog, 
    QPushButton, QHBoxLayout, QAction, QToolBar, QMessageBox
)
from PySide2.QtCore import Qt
from form_designer import FormDesignerWindow, FieldType, WidgetConfig


class FunctionManualDialog(QDialog):
    """功能说明文档对话框"""
    
    def __init__(self, parent=None):
        super().__init__(parent)
        self.setWindowTitle("表单设计器 - 功能说明")
        self.resize(900, 700)
        self._build_ui()
    
    def _build_ui(self):
        """构建界面"""
        layout = QVBoxLayout(self)
        
        # 文本显示区域
        text_edit = QTextEdit()
        text_edit.setReadOnly(True)
        text_edit.setMarkdown(self._get_manual_content())
        layout.addWidget(text_edit)
        
        # 按钮区域
        btn_layout = QHBoxLayout()
        
        # 打印按钮
        print_btn = QPushButton("打印文档")
        print_btn.clicked.connect(self._print_manual)
        btn_layout.addWidget(print_btn)
        
        btn_layout.addStretch()
        
        # 关闭按钮
        close_btn = QPushButton("关闭")
        close_btn.clicked.connect(self.close)
        btn_layout.addWidget(close_btn)
        
        layout.addLayout(btn_layout)
    
    def _get_manual_content(self) -> str:
        """获取说明文档内容"""
        return """# 表单设计器功能说明

## 一、菜单栏功能

### 1. 文件菜单

| 菜单项 | 快捷键 | 功能说明 |
|--------|--------|----------|
| **新建** | Ctrl+N | 创建新的表单设计，清空当前画布。如果当前有未保存的更改，会弹出确认对话框。 |
| **打开...** | Ctrl+O | 从 JSON 文件加载表单配置。支持加载之前保存的表单布局。 |
| **保存** | Ctrl+S | 将当前表单配置保存为 JSON 文件，包含所有控件的位置、属性、数据源配置等。 |
| **退出** | - | 关闭应用程序。 |

### 2. 编辑菜单

| 菜单项 | 功能说明 |
|--------|----------|
| **清空画布** | 删除画布上的所有控件。操作前会弹出确认对话框防止误操作。 |

### 3. 视图菜单

| 菜单项 | 快捷键 | 功能说明 |
|--------|--------|----------|
| **显示网格** | - | 切换画布网格的显示/隐藏。默认开启，网格有助于对齐控件。 |
| **吸附到网格** | - | 切换网格吸附功能的开启/关闭。默认开启，拖动控件时会自动吸附到最近的网格点（10像素间隔）。 |

### 4. 对齐菜单

用于对**选中的多个控件**进行批量对齐操作。

| 菜单项 | 功能说明 |
|--------|----------|
| **左对齐** | 将所有选中控件的左边缘对齐到最左侧控件的左边缘 |
| **右对齐** | 将所有选中控件的右边缘对齐到最右侧控件的右边缘 |
| **顶部对齐** | 将所有选中控件的顶部对齐到最上方控件的顶部 |
| **底部对齐** | 将所有选中控件的底部对齐到最下方控件的底部 |
| **水平居中** | 将所有选中控件在水平方向上居中对齐 |
| **垂直居中** | 将所有选中控件在垂直方向上居中对齐 |

**使用步骤：**
1. 按住 Ctrl 键点击多个控件，或使用框选功能选择多个控件
2. 点击对齐菜单中的相应选项
3. 选中的控件会自动对齐

### 5. 预览菜单

| 菜单项 | 功能说明 |
|--------|----------|
| **预览表单** | 打开预览窗口，查看设计好的表单在实际使用中的效果。可以测试数据输入和获取表单值。 |

### 6. 帮助菜单

| 菜单项 | 功能说明 |
|--------|----------|
| **功能说明** | 打开本文档，查看所有功能的详细说明 |

---

## 二、工具栏按钮

### 文件操作组
- **新建**：同菜单栏"新建"
- **打开**：同菜单栏"打开..."
- **保存**：同菜单栏"保存"
- **清空**：同菜单栏"清空画布"

### 视图控制组
- **网格**（切换按钮）：快速开启/关闭网格显示
  - 按下状态：显示网格
  - 弹起状态：隐藏网格
  
- **吸附**（切换按钮）：快速开启/关闭网格吸附
  - 按下状态：开启吸附
  - 弹起状态：关闭吸附

### 对齐操作组
- **左对齐**：同菜单栏"左对齐"
- **右对齐**：同菜单栏"右对齐"
- **顶对齐**：同菜单栏"顶部对齐"
- **底对齐**：同菜单栏"底部对齐"

### 预览组
- **预览**：同菜单栏"预览表单"

---

## 三、左侧工具箱

### 基础控件

| 控件名称 | 功能说明 |
|----------|----------|
| **文本输入** | 单行文本输入框，适用于输入关键词、名称等短文本。支持占位符提示。 |
| **数字输入** | 数值输入框，支持小数，适用于金额、数量等数值类型数据。 |
| **日期选择** | 带日历弹出的日期选择器，适用于选择日期范围。 |
| **标签** | 纯文本显示，用于说明或分组标题。 |

### 选择控件

| 控件名称 | 功能说明 |
|----------|----------|
| **下拉单选** | 下拉列表，单选模式。适用于选项较少的选择场景（如状态、分类）。支持静态数据、SQL查询、API调用三种数据源。 |
| **下拉多选** | 下拉列表，多选模式。适用于需要选择多个选项的场景。支持滚动查看所有选项。 |
| **搜索单选** | 带搜索功能的下拉选择，输入关键词实时过滤选项。适用于选项较多的场景（如用户选择、商品选择）。支持单选和多选两种模式。 |
| **复选框** | 单个复选框，适用于布尔值选择（是/否）。 |
| **单选组** | 一组互斥的单选按钮，适用于多选一的场景。 |

### 操作控件

| 控件名称 | 功能说明 |
|----------|----------|
| **按钮** | 操作按钮，可配置点击事件。适用于提交、重置等操作。 |

**使用方法：**
1. 在工具箱中找到需要的控件
2. 按住鼠标左键拖动到画布上
3. 释放鼠标，控件会出现在画布上

---

## 四、画布区域操作

### 1. 放置控件

**从工具箱拖放：**
- 按住工具箱中的控件按钮
- 拖动到画布的任意位置
- 释放鼠标，控件会放置在释放位置
- 如果开启了网格吸附，控件会自动对齐到网格

### 2. 移动控件

**自由拖动：**
1. 在画布上点击选中要移动的控件（控件会显示蓝色边框）
2. 按住鼠标左键拖动控件到新位置
3. 释放鼠标完成移动

**对齐辅助线：**
- 拖动时会自动显示对齐辅助线（红色虚线）
- 辅助线显示与其他控件的对齐关系
- 包括：左对齐、右对齐、顶部对齐、底部对齐、水平居中、垂直居中

**网格吸附：**
- 开启网格吸附时，控件会自动吸附到最近的网格点
- 方便精确对齐

### 3. 选中控件

**单选：**
- 直接点击控件即可选中
- 选中的控件会显示蓝色边框

**多选：**
- 按住 Ctrl 键点击多个控件
- 或使用框选功能：在空白处按住鼠标左键拖动，框选多个控件

**取消选择：**
- 点击画布空白区域取消所有选择

### 4. 键盘操作

| 按键 | 功能说明 |
|------|----------|
| **方向键** | 微调选中控件的位置，每次移动 10 像素 |
| **Shift + 方向键** | 精细调整，每次移动 1 像素 |
| **Delete** | 删除选中的控件 |

### 5. 删除控件

**方法：**
1. 选中控件
2. 按 Delete 键
3. 控件被移除

---

## 五、右侧属性面板

### 基础属性

| 属性 | 说明 |
|------|------|
| **字段名** | 控件的唯一标识符，用于代码中引用该控件。建议使用英文和下划线。 |
| **显示标签** | 控件前面显示的标签文本，说明该控件的用途。 |
| **必填** | 标记该字段是否为必填项。选中后标签会显示红色星号。 |
| **占位符** | 输入框中的提示文本，当用户未输入时显示。 |

### 数据源配置（选择类控件）

适用于下拉选择、搜索选择等控件。

| 数据源类型 | 说明 |
|------------|------|
| **静态数据** | 直接在配置中定义选项列表。格式：显示文本=值，每行一个选项。 |
| **SQL查询** | 通过 SQL 查询从数据库获取选项。支持参数绑定。 |
| **API调用** | 通过 HTTP API 获取选项。支持 GET/POST 方法。 |
| **函数调用** | 调用 Python 函数动态获取选项。 |

**静态数据配置示例：**
```
全部=
技术=tech
产品=product
设计=design
```

**SQL查询示例：**
```sql
SELECT id as value, username as label 
FROM users 
WHERE username LIKE :keyword 
LIMIT 20
```

### 样式配置

| 属性 | 说明 |
|------|------|
| **宽度** | 控件的宽度（像素），范围 100-500。 |
| **高度** | 显示"高度自适应内容"，高度根据控件内容自动调整。 |

### 应用更改

编辑完属性后，点击"应用更改"按钮使更改生效。

---

## 六、高度自适应功能说明

本次更新的主要特性是**控件高度自适应内容**：

### 改进内容

1. **控件默认宽度调整为 220 像素**（原来是 200 像素）
2. **控件高度自适应内容**，不再固定为 60 像素
3. **移除了属性面板中的高度设置**，改为显示"高度自适应内容"提示
4. **不同控件根据内容自动调整高度**：

| 控件类型 | 高度行为 |
|----------|----------|
| **文本输入** | 单行输入框的自然高度 |
| **数字输入** | 与文本输入类似，单行高度 |
| **日期选择** | 标准的日期选择器高度 |
| **下拉单选** | 下拉框的自然高度 |
| **下拉多选** | 最大 100 像素，超出显示滚动条 |
| **搜索选择** | 搜索框 + 结果显示区域的自然高度 |
| **复选框** | 单个复选框的自然高度 |
| **单选组** | 根据选项数量自适应 |
| **标签** | 文本内容的自然高度 |
| **按钮** | 按钮内容的自然高度 |

### 优势

- **更美观**：控件高度与内容匹配，不会过高或过低
- **更灵活**：不同控件可以有不同高度，不受固定尺寸限制
- **更友好**：多选列表等内容多的控件会自动扩展，单行控件保持紧凑
- **更易用**：用户无需手动调整高度，系统会自动处理

---

## 七、使用流程示例

### 示例1：创建简单的查询表单

1. **拖放控件**：
   - 从工具箱拖动"文本输入"到画布
   - 拖动"下拉单选"到画布
   - 拖动"日期选择"到画布
   - 拖动"按钮"到画布

2. **配置属性**：
   - 选中文本输入，设置标签为"关键词"
   - 选中下拉单选，设置标签为"分类"，配置静态数据源
   - 选中日期选择，设置标签为"开始日期"
   - 选中按钮，设置标签为"查询"

3. **调整布局**：
   - 拖动控件到合适位置
   - 使用方向键微调位置
   - 或选中多个控件进行对齐

4. **保存配置**：
   - 点击"文件" -> "保存"
   - 输入文件名，保存为 JSON

5. **预览测试**：
   - 点击"预览"查看效果
   - 测试输入和选择功能

### 示例2：创建带搜索的复杂表单

1. **添加搜索选择**：
   - 拖动"搜索单选"到画布
   - 设置标签为"选择用户"
   - 配置数据源为 SQL 或 API

2. **配置搜索功能**：
   - 在属性面板选择数据源类型为"SQL查询"
   - 输入查询语句，使用 `:keyword` 作为搜索参数占位符

3. **测试搜索**：
   - 在预览窗口中输入关键词
   - 观察是否显示匹配的选项

---

## 八、快捷键汇总

| 快捷键 | 功能 |
|--------|------|
| Ctrl+N | 新建表单 |
| Ctrl+O | 打开表单配置 |
| Ctrl+S | 保存表单配置 |
| Delete | 删除选中控件 |
| 方向键 | 微调控件位置（10像素） |
| Shift + 方向键 | 精细调整（1像素） |
| F1 | 打开功能说明文档 |

---

## 九、常见问题

**Q: 控件放置后不能移动？**
A: 确保点击选中控件后再拖动。如果还不能移动，检查是否选中了控件。

**Q: 如何精确对齐控件？**
A: 开启"吸附到网格"功能，或使用对齐菜单。也可以选中后用方向键微调。

**Q: 下拉选择如何配置数据？**
A: 选中控件，在属性面板找到"数据源配置"，选择数据源类型并输入相应配置。

**Q: 如何同时选择多个控件？**
A: 按住 Ctrl 键点击控件，或在空白处拖动鼠标进行框选。

**Q: 控件高度可以调整吗？**
A: 控件高度自动适应内容，不可手动调整。只可以调整宽度。

**Q: 为什么移除了高度设置？**
A: 高度自适应是本次更新的主要特性，让控件根据内容自动调整高度，使界面更美观、使用更方便。

---

## 十、注意事项

1. **保存工作**：建议经常保存，防止意外丢失
2. **字段命名**：使用有意义的英文字段名，便于后续代码使用
3. **数据源测试**：配置 SQL 或 API 后，务必在预览中测试数据加载
4. **布局美观**：建议使用网格对齐，保持表单整齐
5. **多选操作**：善用多选和对齐功能，快速整理布局

---

**版本**: 1.1  
**更新日期**: 2026-02-09  
**更新内容**: 添加高度自适应功能  
**适用系统**: Windows 7+, Python 3.8+, PySide2
"""
    
    def _print_manual(self):
        """打印文档到控制台"""
        print("\n" + "=" * 80)
        print("表单设计器功能说明")
        print("=" * 80)
        print(self._get_manual_content())
        print("=" * 80 + "\n")


class TestWindow(FormDesignerWindow):
    """测试窗口 - 添加了帮助菜单"""
    
    def __init__(self):
        super().__init__()
        self.setWindowTitle("表单设计器 - 高度自适应测试版")
        self._build_help_menu()
    
    def _build_help_menu(self):
        """构建帮助菜单"""
        # 获取菜单栏
        menubar = self.menuBar()
        
        # 帮助菜单
        help_menu = menubar.addMenu("帮助")
        
        # 功能说明
        manual_action = QAction("功能说明(F1)", self)
        manual_action.triggered.connect(self._show_manual)
        help_menu.addAction(manual_action)
        
        # 关于
        about_action = QAction("关于", self)
        about_action.triggered.connect(self._show_about)
        help_menu.addAction(about_action)
        
        # 在工具栏添加帮助按钮
        toolbar = self.findChild(QToolBar)
        if toolbar:
            toolbar.addSeparator()
            toolbar.addAction("帮助", self._show_manual)
    
    def _show_manual(self):
        """显示功能说明文档"""
        dialog = FunctionManualDialog(self)
        dialog.exec_()
    
    def _show_about(self):
        """显示关于对话框"""
        from PySide2.QtWidgets import QMessageBox
        QMessageBox.about(
            self,
            "关于表单设计器",
            """<h2>表单设计器 v1.1</h2>
            <p><b>主要功能：</b></p>
            <ul>
                <li>可视化表单设计</li>
                <li>控件自由拖放排布</li>
                <li>高度自适应内容</li>
                <li>网格对齐辅助</li>
                <li>多种数据源支持</li>
            </ul>
            <p><b>技术栈：</b>Python 3.8+, PySide2, Qt5</p>
            <p><b>更新日期：</b>2026-02-09</p>
            <p>按 F1 或点击"帮助"菜单查看完整功能说明</p>
            """
        )


def main():
    """测试自适应高度"""
    app = QApplication(sys.argv)
    app.setStyle("Fusion")
    
    window = TestWindow()
    window.show()
    
    print("=" * 60)
    print("控件高度自适应功能已启用")
    print("=" * 60)
    print()
    print("改进内容：")
    print("1. 控件默认宽度调整为 220 像素")
    print("2. 控件高度自适应内容，不再固定为 60 像素")
    print("3. 移除了属性面板中的高度设置（改为显示'高度自适应内容'）")
    print("4. 不同控件根据内容自动调整高度：")
    print("   - 文本输入：单行高度")
    print("   - 下拉选择：适当高度")
    print("   - 多选列表：最大 100 像素，支持滚动")
    print("   - 搜索选择：根据搜索框和结果显示自适应")
    print("   - 复选框/单选组：根据选项数量自适应")
    print()
    print("新增功能：")
    print("- 帮助菜单（包含功能说明文档）")
    print("- 按 F1 键快速打开功能说明")
    print("- 工具栏添加帮助按钮")
    print()
    print("操作提示：")
    print("- 拖放控件到画布，观察高度自适应效果")
    print("- 只有宽度可以调整，高度自动适应内容")
    print("- 点击'帮助' -> '功能说明' 查看完整文档")
    print()
    print("=" * 60)
    
    sys.exit(app.exec_())


if __name__ == "__main__":
    main()
